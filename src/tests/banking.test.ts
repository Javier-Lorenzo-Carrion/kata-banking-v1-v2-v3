import { expect } from '@jest/globals';
import { bankingV1 } from '../core/banking';

describe('Banking version 1 should', () => {
  it('something', () => {
    expect(bankingV1()).toBe('');
  });
});
